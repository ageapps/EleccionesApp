<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="0dip" />
            <gradient
                android:startColor="@color/color_light"
                android:centerColor="@color/color_light"
                android:centerY="0.75"
                android:endColor="@color/color_light"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="0dip" />
                <gradient
                    android:startColor="@color/colorCandidatos"
                    android:centerColor="@color/primaryColor"
                    android:centerY="0.75"
                    android:endColor="@color/primaryDarkColor"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="0dip" />
                <gradient
                    android:startColor="@color/colorCandidatos"
                    android:centerColor="@color/primaryColor"
                    android:centerY="0.75"
                    android:endColor="@color/primaryDarkColor"
                    android:angle="270" />
            </shape>
        </clip>
    </item>

</layer-list>